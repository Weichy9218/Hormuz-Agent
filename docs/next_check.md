# Next Check

Last updated: 2026-05-12

## What Changed

- Added `audit:replay` and included it in `npm run audit`.
- Clarified `TargetForecast.sourceIds`: registry source ids only; observation lineage stays in `sourceObservationIds`.
- Reworked Overview so the lead card explains the judgement update instead of making `54%` the visual center.
- Added a compact Overview workflow strip that maps the original PDF plan to the current pages: `News -> Market -> Forecast -> Overview`.
- Expanded the top-level IA to four pages: Overview / Market / News / Forecast.
- Added Market key-series cards with sampled FRED trend lines and an analysis window.
- Added News timeline and forecast handoff sections.
- Added Forecast agent stages to explain the galaxy-style flow: Sense / Interpret / Revise / Persist.
- Removed obsolete `dailyBriefs`, legacy `baseScenarios`, and unused old UI styles so the active code path follows the canonical-store version.

## Original Plan Alignment

The initial PDF direction is preserved as a focused case room:

```text
态势时间线 -> 关键指标面板 -> 机制解释链 -> 情景预测卡片
```

Current implementation maps that to:

```text
News timeline -> Market key indicators -> Forecast evidence graph -> Overview scenario state
```

Overview remains first because reviewers need the current judgement immediately, but it no longer makes `Controlled disruption 54%` the unexplained center of gravity.

## Current System Shape

```text
registered source
  -> source observation
  -> evidence claim
  -> mechanism tag
  -> judgement_updated
  -> target forecast
  -> checkpoint
  -> PredictionRecord[]
```

The UI is still a reviewer-facing deep case surface, not a live AIS system, news aggregator, or trading interface.

## Verification

Latest local checks passed:

```bash
npm run lint
npm run build
npm run audit
```

`audit:replay` compares deterministic replay outputs for current state, target forecasts, deltas, attribution, guardrails, sensitivity, and `PredictionRecord[]`.

## Known Limits

- Market charts use registered sampled FRED points, not full one-year daily K-lines yet.
- The Market analysis window is not a verified closure date.
- News timeline uses local registered narrative events; it is not live news ingestion.
- Replay mode has script-level audit, but not an interactive UI playback yet.

## Best Next Steps

1. Add `audit:ui` for visible caveats, as-of labels, and pending-source markers.
2. Implement Forecast Replay mode using `AgentRunEvent[]`.
3. Replace sampled FRED points with a versioned one-year fixture generated by an audited data script.
4. Connect galaxy / galaxy-selfevolve output artifacts as read-only fixtures.
5. Add browser screenshot regression for Overview / Market / News / Forecast.
