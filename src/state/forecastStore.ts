// Re-export of canonical labels used by UI components.
// This file no longer owns business state — the canonical store does.
export {
  scenarioOrder,
  scenarioLabel,
  scenarioEnglishLabel,
  scenarioPosture,
  scenarioColor,
  forecastTargetOptions,
  targetLabel,
} from "./canonicalStore";
